version: '3.8'

services:
  mongodb:
    image: mongo:8.0
    container_name: daoshu-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=daoist_agent

volumes:
  mongo-data:
    driver: local
